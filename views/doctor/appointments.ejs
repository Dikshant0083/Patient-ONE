<!DOCTYPE html>
<html lang="en">
<head>
    <title>My Appointments</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/dashboard.css">
    <style>
        /* Extra Appointments Styles */
        .appointment-card {
            background: white;
            border-radius: 0.75rem;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
            border-left: 4px solid #06b6d4;
            transition: all 0.3s ease;
        }

        .appointment-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(0,0,0,0.1);
        }

        .appointment-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 1rem;
            padding-bottom: 1rem;
            border-bottom: 1px solid #e5e7eb;
        }

        .appointment-patient {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .appointment-avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: linear-gradient(135deg, #06b6d4, #0891b2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 700;
            font-size: 1.25rem;
        }

        .patient-info h4 {
            font-size: 1.1rem;
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 0.25rem;
        }

        .patient-info p {
            color: #6b7280;
            font-size: 0.9rem;
            margin: 0;
        }

        .appointment-status {
            display: inline-block;
            padding: 0.4rem 0.8rem;
            border-radius: 0.4rem;
            font-weight: 600;
            font-size: 0.8rem;
        }

        .status-pending {
            background: #fef3c7;
            color: #92400e;
        }

        .status-approved {
            background: #dcfce7;
            color: #166534;
        }

        .status-rejected {
            background: #fee2e2;
            color: #991b1b;
        }

        .appointment-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 1.5rem;
            margin: 1.5rem 0;
        }

        .detail-item {
            background: #f9fafb;
            padding: 1rem;
            border-radius: 0.5rem;
        }

        .detail-label {
            color: #6b7280;
            font-size: 0.85rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .detail-label i {
            color: #06b6d4;
        }

        .detail-value {
            font-size: 1rem;
            font-weight: 600;
            color: #1f2937;
        }

        .appointment-reason {
            background: #e0f2fe;
            padding: 1rem;
            border-radius: 0.5rem;
            border-left: 3px solid #06b6d4;
            margin: 1rem 0;
            color: #1e40af;
        }

        .appointment-reason-label {
            font-weight: 600;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .appointment-actions {
            display: flex;
            gap: 1rem;
            margin-top: 1.5rem;
            padding-top: 1.5rem;
            border-top: 1px solid #e5e7eb;
        }

        .btn-approve,
        .btn-reject {
            flex: 1;
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            border: none;
            font-weight: 600;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .btn-approve {
            background: linear-gradient(135deg, #16a34a, #15803d);
            color: white;
        }

        .btn-approve:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(22, 163, 74, 0.3);
            color: white;
        }

        .btn-reject {
            background: #fee2e2;
            color: #991b1b;
        }

        .btn-reject:hover {
            background: #fecaca;
            transform: translateY(-2px);
            color: #991b1b;
        }

        .no-action {
            text-align: center;
            padding: 1rem;
            color: #9ca3af;
            font-size: 0.9rem;
        }

        .empty-state {
            text-align: center;
            padding: 3rem 2rem;
        }

        .empty-icon {
            font-size: 4rem;
            color: #d1d5db;
            margin-bottom: 1rem;
        }

        .empty-text {
            color: #6b7280;
            font-size: 1.1rem;
        }

        @media (max-width: 768px) {
            .appointment-header {
                flex-direction: column;
                gap: 1rem;
            }

            .appointment-details {
                grid-template-columns: 1fr;
            }

            .appointment-actions {
                flex-direction: column;
            }

            .btn-approve,
            .btn-reject {
                width: 100%;
            }
        }
    </style>
</head>
<body>

<div class="main-container">
    <!-- SIDEBAR -->
    <aside class="sidebar">
        <div class="sidebar-logo">
            <h2>MediCare</h2>
            <p>Doctor Portal</p>
        </div>

        <ul class="sidebar-nav">
            <li><a href="/doctor/dashboard"><i class="fas fa-home"></i> Dashboard</a></li>
            <li><a href="/doctor/appointments" class="active"><i class="fas fa-calendar-alt"></i> Appointments</a></li>
            <li><a href="/profile"><i class="fas fa-user"></i> My Profile</a></li>
            <li><a href="/auth/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
        </ul>
    </aside>

    <!-- CONTENT -->
    <div class="content-wrapper">

        <!-- Header -->
        <div class="dashboard-header">
            <div>
                <h1>My Appointments</h1>
                <p>Manage your consultation requests</p>
            </div>
            <div class="header-user">
                <div>
                    <p style="margin-bottom: 0; font-weight: 600;">Dr. <%= user.name || user.email %></p>
                    <small style="color: #9ca3af;">Medical Professional</small>
                </div>
                <div class="user-avatar">
                    <%= (user.name || user.email).charAt(0).toUpperCase() %>
                </div>
            </div>
        </div>

        <!-- Appointments List -->
        <div class="dashboard-card">
            <div class="card-header-section">
                <h2 class="card-title"><i class="fas fa-calendar-check"></i> Appointment Requests</h2>
                <a href="/doctor/dashboard" class="view-all-btn">
                    <i class="fas fa-arrow-left"></i> Back
                </a>
            </div>

            <% if (appointments && appointments.length > 0) { %>
                <% appointments.forEach(a => { %>
                    <div class="appointment-card">
                        <!-- Header -->
                        <div class="appointment-header">
                            <div class="appointment-patient">
                                <div class="appointment-avatar">
                                    <%= (a.patientId.name || a.patientId.email).charAt(0).toUpperCase() %>
                                </div>
                                <div class="patient-info">
                                    <h4><%= a.patientId.name || 'Patient' %></h4>
                                    <p><%= a.patientId.email %></p>
                                </div>
                            </div>
                            <span class="appointment-status status-<%= a.status %>">
                                <% if (a.status === 'pending') { %>
                                    <i class="fas fa-clock"></i> Pending
                                <% } else if (a.status === 'approved') { %>
                                    <i class="fas fa-check-circle"></i> Approved
                                <% } else { %>
                                    <i class="fas fa-times-circle"></i> Rejected
                                <% } %>
                            </span>
                        </div>

                        <!-- Details -->
                        <div class="appointment-details">
                            <div class="detail-item">
                                <div class="detail-label"><i class="fas fa-calendar"></i> Date</div>
                                <div class="detail-value"><%= a.date %></div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label"><i class="fas fa-clock"></i> Time</div>
                                <div class="detail-value"><%= a.time %></div>
                            </div>
                            <div class="detail-item">
                                <div class="detail-label"><i class="fas fa-hourglass-end"></i> Status</div>
                                <div class="detail-value" style="text-transform: capitalize;">
                                    <%= a.status %>
                                </div>
                            </div>
                        </div>

                        <!-- Reason -->
                        <% if (a.reason && a.reason.trim() !== '') { %>
                            <div class="appointment-reason">
                                <div class="appointment-reason-label">
                                    <i class="fas fa-stethoscope"></i> Reason for Consultation
                                </div>
                                <p style="margin: 0;"><%= a.reason %></p>
                            </div>
                        <% } %>

                        <!-- Actions (Only show for pending) -->
                        <% if (a.status === 'pending') { %>
                            <div class="appointment-actions">
                                <a href="/doctor/appointment/<%= a._id %>/approve" class="btn-approve">
                                    <i class="fas fa-check"></i> Approve
                                </a>
                                <a href="/doctor/appointment/<%= a._id %>/reject" class="btn-reject">
                                    <i class="fas fa-times"></i> Reject
                                </a>
                            </div>
                        <% } else { %>
                            <div class="no-action">
                                <i class="fas fa-info-circle"></i>
                                This appointment has been <%= a.status === 'approved' ? 'approved' : 'rejected' %>.
                                No further action needed.
                            </div>
                        <% } %>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="empty-state">
                    <div class="empty-icon"><i class="fas fa-inbox"></i></div>
                    <p class="empty-text">No appointments scheduled</p>
                </div>
            <% } %>
        </div>

        <!-- Footer Navigation -->
        <div class="footer-nav">
            <a href="/doctor/dashboard" class="nav-btn profile">
                <i class="fas fa-arrow-left"></i> Back to Dashboard
            </a>
            <a href="/auth/logout" class="nav-btn logout">
                <i class="fas fa-sign-out-alt"></i> Logout
            </a>
        </div>

    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
    // Active link highlighting
    document.querySelectorAll('.sidebar-nav a').forEach(link => {
        if (link.href === window.location.href) {
            link.classList.add('active');
        }
    });
</script>

</body>
</html>