<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #f3f4f6 0%, #e5e7eb 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #1f2937;
            min-height: 100vh;
            padding: 2rem 1rem;
        }

        .payment-history-wrapper {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Header Section */
        .history-header {
            background: white;
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
            margin-bottom: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .history-header h1 {
            font-size: 2rem;
            font-weight: 700;
            color: #1f2937;
            display: flex;
            align-items: center;
            gap: 1rem;
            margin: 0;
        }

        .history-header h1 i {
            color: #06b6d4;
        }

        .back-btn {
            background: #f3f4f6;
            color: #374151;
            padding: 0.75rem 1.5rem;
            border-radius: 0.5rem;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .back-btn:hover {
            background: #e5e7eb;
            color: #1f2937;
            transform: translateY(-2px);
        }

        /* Summary Cards */
        .summary-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .summary-card {
            background: white;
            padding: 1.5rem;
            border-radius: 1rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
            transition: all 0.3s ease;
        }

        .summary-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 16px rgba(0,0,0,0.1);
        }

        .summary-icon {
            width: 50px;
            height: 50px;
            border-radius: 0.75rem;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        .summary-icon.green {
            background: #dcfce7;
            color: #16a34a;
        }

        .summary-icon.blue {
            background: #dbeafe;
            color: #2563eb;
        }

        .summary-icon.orange {
            background: #fed7aa;
            color: #ea580c;
        }

        .summary-number {
            font-size: 2rem;
            font-weight: 700;
            color: #1f2937;
            margin-bottom: 0.5rem;
        }

        .summary-label {
            color: #6b7280;
            font-size: 0.875rem;
            font-weight: 500;
        }

        /* Payment Table */
        .payment-table-card {
            background: white;
            border-radius: 1rem;
            padding: 2rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        }

        .payment-table-card h2 {
            font-size: 1.25rem;
            font-weight: 700;
            color: #1f2937;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .payment-table-card h2 i {
            color: #06b6d4;
        }

        .table-wrapper {
            overflow-x: auto;
        }

        .payments-table {
            width: 100%;
            border-collapse: collapse;
        }

        .payments-table thead {
            background: #f9fafb;
            border-bottom: 2px solid #e5e7eb;
        }

        .payments-table th {
            padding: 1rem;
            text-align: left;
            font-weight: 600;
            color: #374151;
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .payments-table td {
            padding: 1rem;
            border-bottom: 1px solid #e5e7eb;
            color: #6b7280;
        }

        .payments-table tbody tr {
            transition: background 0.2s ease;
        }

        .payments-table tbody tr:hover {
            background: #f9fafb;
        }

        .status-badge {
            display: inline-block;
            padding: 0.4rem 0.8rem;
            border-radius: 0.4rem;
            font-weight: 600;
            font-size: 0.8rem;
        }

        .badge-completed {
            background: #dcfce7;
            color: #166534;
        }

        .badge-pending {
            background: #fef3c7;
            color: #92400e;
        }

        .badge-failed {
            background: #fee2e2;
            color: #991b1b;
        }

        .amount-cell {
            font-weight: 700;
            font-size: 1.1rem;
            color: #16a34a;
        }

        .transaction-id {
            font-family: 'Courier New', monospace;
            font-size: 0.85rem;
            color: #6b7280;
            background: #f9fafb;
            padding: 0.25rem 0.5rem;
            border-radius: 0.25rem;
        }

        .empty-state {
            text-align: center;
            padding: 3rem 1rem;
        }

        .empty-icon {
            font-size: 4rem;
            color: #d1d5db;
            margin-bottom: 1rem;
        }

        .empty-text {
            color: #6b7280;
            font-size: 1.1rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .history-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }

            .history-header h1 {
                font-size: 1.5rem;
            }

            .back-btn {
                width: 100%;
                justify-content: center;
            }

            .payments-table th,
            .payments-table td {
                padding: 0.75rem 0.5rem;
                font-size: 0.85rem;
            }
        }
    </style>
</head>
<body>

<div class="payment-history-wrapper">
    <!-- Header -->
    <div class="history-header">
        <h1><i class="fas fa-credit-card"></i> Payment History</h1>
        <a href="/patient/dashboard" class="back-btn">
            <i class="fas fa-arrow-left"></i> Back to Dashboard
        </a>
    </div>

    <!-- Summary Cards -->
    <div class="summary-cards">
        <div class="summary-card">
            <div class="summary-icon green">
                <i class="fas fa-rupee-sign"></i>
            </div>
            <div class="summary-number">
                ₹<%= payments.filter(p => p.status === 'completed').reduce((sum, p) => sum + p.amount, 0) %>
            </div>
            <div class="summary-label">Total Paid</div>
        </div>

        <div class="summary-card">
            <div class="summary-icon blue">
                <i class="fas fa-check-circle"></i>
            </div>
            <div class="summary-number">
                <%= payments.filter(p => p.status === 'completed').length %>
            </div>
            <div class="summary-label">Successful Payments</div>
        </div>

        <div class="summary-card">
            <div class="summary-icon orange">
                <i class="fas fa-calendar-check"></i>
            </div>
            <div class="summary-number">
                <%= payments.length %>
            </div>
            <div class="summary-label">Total Transactions</div>
        </div>
    </div>

    <!-- Payment Table -->
    <div class="payment-table-card">
        <h2><i class="fas fa-list"></i> All Transactions</h2>

        <div class="table-wrapper">
            <% if (payments && payments.length > 0) { %>
                <table class="payments-table">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th>Doctor</th>
                            <th>Appointment</th>
                            <th>Amount</th>
                            <th>Status</th>
                            <th>Transaction ID</th>
                            <th>Payment Method</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% payments.forEach(payment => { %>
                            <tr>
                                <td>
                                    <%= new Date(payment.createdAt).toLocaleDateString('en-IN', {
                                        day: 'numeric',
                                        month: 'short',
                                        year: 'numeric'
                                    }) %>
                                    <br>
                                    <small style="color: #9ca3af;">
                                        <%= new Date(payment.createdAt).toLocaleTimeString('en-IN', {
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %>
                                    </small>
                                </td>
                                <td>
                                    <strong>Dr. <%= payment.doctorId.name || payment.doctorId.email %></strong>
                                    <br>
                                    <small style="color: #9ca3af;"><%= payment.doctorId.email %></small>
                                </td>
                                <td>
                                    <% if (payment.appointmentId) { %>
                                        <%= payment.appointmentId.date %>
                                        <br>
                                        <small style="color: #9ca3af;"><%= payment.appointmentId.time %></small>
                                    <% } else { %>
                                        <span style="color: #9ca3af;">—</span>
                                    <% } %>
                                </td>
                                <td class="amount-cell">₹<%= payment.amount %></td>
                                <td>
                                    <% if (payment.status === 'completed') { %>
                                        <span class="status-badge badge-completed">
                                            <i class="fas fa-check-circle"></i> Completed
                                        </span>
                                    <% } else if (payment.status === 'pending') { %>
                                        <span class="status-badge badge-pending">
                                            <i class="fas fa-clock"></i> Pending
                                        </span>
                                    <% } else { %>
                                        <span class="status-badge badge-failed">
                                            <i class="fas fa-times-circle"></i> Failed
                                        </span>
                                    <% } %>
                                </td>
                                <td>
                                    <% if (payment.razorpayPaymentId) { %>
                                        <span class="transaction-id" title="<%= payment.razorpayPaymentId %>">
                                            <%= payment.razorpayPaymentId.substring(0, 15) %>...
                                        </span>
                                    <% } else { %>
                                        <span style="color: #9ca3af;">N/A</span>
                                    <% } %>
                                </td>
                                <td>
                                    <i class="fas fa-credit-card" style="color: #06b6d4; margin-right: 0.5rem;"></i>
                                    <%= payment.paymentMethod || 'Razorpay' %>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } else { %>
                <div class="empty-state">
                    <div class="empty-icon"><i class="fas fa-receipt"></i></div>
                    <p class="empty-text">No payment history yet. Book an appointment to get started.</p>
                </div>
            <% } %>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>